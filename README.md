# useContext task at Kodehode

- Use React to create a multipage counter site.
- Use useContext to globally store count.
- Showcase in some way that count on multiple pages
- Have the count update through all pages